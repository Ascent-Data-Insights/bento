import { Heading } from '../components/heading'
import { Text } from '../components/text'

export function RoutesPage() {
  return (
    <div>
      <Heading className="font-heading">Routes</Heading>
      <Text className="mt-2">No routes optimized yet. Load jobs and hit optimize to see routes.</Text>
    </div>
  )
}
